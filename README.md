# ClaudeCode Config Manager (Tauri 版本)

🚀 **全新 Tauri 架构 - 更快、更轻、更强！**

## ✨ 性能对比

| 项目 | Electron 版本 | Tauri 版本 | 提升 |
|------|-------------|-----------|------|
| 📦 包体积 | ~100 MB | ~5 MB | **20倍** |
| ⚡ 启动速度 | ~3-5 秒 | ~0.3 秒 | **10倍+** |
| 💾 内存占用 | ~150 MB | ~50 MB | **3倍** |
| 🌐 网络依赖 | CDN（慢） | 本地资源 | **秒开** |

## 🎯 主要改进

### 1. **架构升级**
- ✅ 从 Electron 迁移到 Tauri
- ✅ 后端使用 Rust（安全、高效）
- ✅ 前端保持不变（HTML/CSS/JS）

### 2. **性能优化**
- ✅ 移除 Tailwind CDN，使用本地版本
- ✅ 移除 Google Fonts，使用系统字体
- ✅ 异步处理配置切换（不阻塞 UI）

### 3. **功能保持**
- ✅ 可视化配置管理
- ✅ 一键切换配置
- ✅ 环境变量直接生效
- ✅ 所有原有功能完整保留

## 🚀 快速开始

### 方式 1：开发模式（推荐测试）

```bash
# 双击运行
启动(开发模式).bat

# 或命令行
cd src-tauri
cargo tauri dev
```

### 方式 2：构建发布版

```bash
# 双击运行
构建发布版.bat

# 或命令行
cd src-tauri
cargo tauri build
```

构建完成后，可执行文件位于：`src-tauri/target/release/claude-config-manager.exe`

## 📁 项目结构

```
claude-config-manager-tauri/
├── src/                    # 前端代码
│   ├── index.html         # 主页面
│   ├── renderer.js        # 前端逻辑（Tauri API）
│   └── tailwind.js        # 本地 Tailwind
├── src-tauri/             # Tauri/Rust 后端
│   ├── src/
│   │   └── main.rs        # Rust 主程序
│   ├── icons/
│   │   └── icon.ico       # 应用图标
│   ├── Cargo.toml         # Rust 依赖配置
│   └── tauri.conf.json    # Tauri 配置
├── package.json           # 项目配置
└── README.md             # 本文件
```

## 🛠️ 技术栈

### 后端
- **Tauri 2.9** - 桌面应用框架
- **Rust 1.89** - 系统编程语言
- **serde** - 序列化/反序列化
- **dirs** - 跨平台目录管理

### 前端
- **HTML5/CSS3** - 界面结构
- **JavaScript (ES6+)** - 前端逻辑
- **Tailwind CSS (本地)** - 样式框架
- **系统字体** - 字体渲染

## 📝 配置文件

配置数据保存位置（与 Electron 版本兼容）：

- `~/.claude/config.json` - ClaudeCode 当前配置
- `~/.claude_configs.json` - 所有保存的配置方案（旧格式）
- `~/claude-configs.cccm` - 所有保存的配置方案（新格式）

Windows 示例：
- `C:\Users\你的用户名\.claude\config.json`
- `C:\Users\你的用户名\.claude_configs.json`

## 🔧 开发

```bash
# 检查 Rust 代码
cd src-tauri
cargo check

# 运行开发模式
cargo tauri dev

# 构建 release 版本
cargo tauri build

# 只构建 debug 版本（更快）
cargo build
```

## 📦 打包产物

运行 `cargo tauri build` 后，会生成：

- `src-tauri/target/release/claude-config-manager.exe` - 可执行文件
- `src-tauri/target/release/bundle/nsis/` - NSIS 安装包
- `src-tauri/target/release/bundle/msi/` - MSI 安装包

## 🎨 界面预览

与 Electron 版本完全一致：
- 🎨 紫色渐变主题
- 📋 左侧配置列表
- 📄 右侧详情面板
- ✨ 流畅动画效果
- 🔔 Toast 通知反馈

## ⚠️ 注意事项

1. **首次构建**：首次运行会下载 Rust 依赖，可能需要 3-5 分钟
2. **环境变量**：切换配置后，新开的 Claude Code 窗口会自动使用新配置
3. **配置兼容**：与 Electron 版本共用配置文件，可以无缝切换

## 🆚 Electron vs Tauri

| 特性 | Electron | Tauri |
|-----|----------|-------|
| 渲染引擎 | Chromium | WebView2 |
| 后端语言 | Node.js | Rust |
| 包体积 | 很大 | 很小 |
| 启动速度 | 慢 | 快 |
| 内存占用 | 高 | 低 |
| 安全性 | 好 | 更好 |

## 📄 许可证

MIT License

## 🙏 致谢

- 感谢 [Tauri](https://tauri.app/) 团队提供的优秀框架
- 感谢 Rust 社区的贡献
- 感谢 ClaudeCode 团队

---

**Made with ❤️ by Zhu Qixuan & Claude**

**从 Electron 到 Tauri - 一次成功的性能革命！** 🚀
